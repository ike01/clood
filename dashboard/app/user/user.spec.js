'use strict';

describe('cloodApp.user module', function() {

  beforeEach(module('cloodApp.user'));

  describe('user controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userCtrl = $controller('UserCtrl');
      expect(userCtrl).toBeDefined();
    }));

  });
});
